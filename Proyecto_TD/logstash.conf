# logstash.conf file to process logs
input {
  tcp {
    port => 5044
  }
}

filter {
  # Example: Add processing or filtering logic here
}

output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
  }
}
